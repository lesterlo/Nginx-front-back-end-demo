# Generated/owned by the C++ NginxController (webengine::NginxController).
# Do not edit by hand — set_listen_port()/set_listen_ports() rewrite this file,
# then validate (nginx -t) and apply it (nginx -s reload).
#
# High ports (>=1024) so nginx can bind them as the unprivileged service user.
listen 8080;
listen 8443 ssl;
