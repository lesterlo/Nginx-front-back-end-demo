info:
  name: Get dashboard
  type: http
  seq: 2

http:
  method: GET
  url: http://{{ipAddr}}:{{port}}/protected/dashboard.html
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
