info:
  name: API-Private-user
  type: http
  seq: 3

http:
  method: GET
  url: https://{{ipAddr}}:{{port}}/api/user
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
