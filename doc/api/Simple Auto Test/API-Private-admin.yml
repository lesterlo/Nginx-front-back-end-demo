info:
  name: API-Private-admin
  type: http
  seq: 2

http:
  method: GET
  url: https://{{ipAddr}}:{{port}}/api/admin
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
