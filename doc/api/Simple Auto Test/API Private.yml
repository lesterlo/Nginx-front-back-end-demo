info:
  name: API Private
  type: http
  seq: 2

http:
  method: GET
  url: https://{{ipAddr}}:{{port}}/api/private
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
