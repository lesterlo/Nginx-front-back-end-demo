info:
  name: API-Private-viewer
  type: http
  seq: 4

http:
  method: GET
  url: https://{{ipAddr}}:{{port}}/api/viewer
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
