info:
  name: Static File Private
  type: http
  seq: 3

http:
  method: GET
  url: http://{{ipAddr}}:{{port}}/protected/hello.txt
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
